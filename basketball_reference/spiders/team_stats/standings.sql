CREATE TABLE standings(
  collected_date DATE,
  team TEXT,
  conference TEXT,
  seed INT,
  wins INT,
  losses INT,
  win_loss_pct REAL,
  gb INT,
  pts_per_g REAL,
  opp_pts_per_g REAL,
  srs REAL
);